import Foundation

// TEMPLATE — copy this file to `Tiles/Secrets.swift` and add your key:
//
//     cp Secrets.example.swift Tiles/Secrets.swift
//
// `Tiles/Secrets.swift` is gitignored so the real key is never committed.
// This template lives at the repo root (outside the Xcode source folder) so it
// is not compiled into the app — otherwise it would clash with the real Secrets.
enum Secrets {
    static let claudeAPIKey = "" // paste your Anthropic API key here
}
